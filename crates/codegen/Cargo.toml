[package]
name = "codegen"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
parser = { path = "../parser" }
validator = { path = "../validator" }

[dev-dependencies]
tempfile = "3"
